import React from 'react'
import { connect } from 'react-redux'
import { withRouter } from 'react-router-dom'

const Forms = props => {
    return (
        <div>
            Forms
        </div>
    )
}

export default withRouter(connect()(Forms))